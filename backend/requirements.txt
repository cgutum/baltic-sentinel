fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# Kafka (Aiven) — H1+
confluent-kafka==2.6.1

# Postgres (Aiven) — H1+. [pool] adds psycopg_pool so state_builder reuses connections
# instead of a fresh TLS handshake per message (that starved the Kafka consumer cross-cloud).
psycopg[binary,pool]==3.2.3
SQLAlchemy==2.0.36

# Geo + data foundation — Person A
shapely==2.0.6
h3==4.1.0
requests==2.32.3

# Agents + voice — Person B, H5+
anthropic==0.42.0
elevenlabs==1.50.3
