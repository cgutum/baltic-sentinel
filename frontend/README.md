# Baltic Sentinel — Frontend

Built later (H21+). Deploy on Vercel. Runs at `http://localhost:3000`, calls
the backend at `http://localhost:8000`.

Screens (only these):
- Replay button
- Live event timeline
- Agent cards
- Final assessment panel
- Voice briefing player
